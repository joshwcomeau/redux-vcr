{
  "name": "redux-vcr",
  "version": "0.1.10",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel index.js --out-file lib/index.js",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Joshua Comeau",
  "license": "MIT",
  "dependencies": {
    "redux-vcr.capture": "^0.1.10",
    "redux-vcr.persist": "^0.1.10",
    "redux-vcr.replay": "^0.1.10",
    "redux-vcr.retrieve": "^0.1.10"
  },
  "babel": {
    "presets": [
      "es2015"
    ],
    "plugins": [
      "transform-object-rest-spread"
    ]
  }
}
