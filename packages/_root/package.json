{
  "name": "redux-vcr",
  "version": "0.2.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel index.js --out-file lib/index.js",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Joshua Comeau",
  "license": "MIT",
  "dependencies": {
    "redux-vcr.capture": "^0.2.0",
    "redux-vcr.persist": "^0.2.0",
    "redux-vcr.replay": "^0.2.0",
    "redux-vcr.retrieve": "^0.2.0"
  },
  "babel": {
    "presets": [
      "es2015"
    ],
    "plugins": [
      "transform-object-rest-spread"
    ]
  }
}
