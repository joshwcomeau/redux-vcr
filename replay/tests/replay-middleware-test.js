/* eslint-disable no-unused-expressions */
import { expect } from 'chai';

import { replayMiddleware } from '../src';


describe('replayMiddleware', () => {
  it('will have tests soon', () => {
    expect(true).to.equal(true);
  });
});
